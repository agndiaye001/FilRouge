pipeline {
    agent any
    stages {
        stage('test') {
            agent { dockerfile 'FilRouge/backend/Dockerfile' }  
            stages {
                stage('Test') {
                    steps {
                        sh 'docker --version'
                    }
                }
            }
        }
    }
}
