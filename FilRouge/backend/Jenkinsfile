pipeline {
    agent any
    stages {
        stage('Build') {
            agent { dockerfile true }  
            stages {
                stage('Test') {
                    steps {
                        sh 'docker --version'
                    }
                }
            }
        }
    }
}
